## What & why

<!-- 1-3 sentences. Why this change, not what (the diff shows that). -->
<!-- For features: link the approved issue (see CONTRIBUTING.md). -->

## Test plan

<!-- How you verified this. `swift test` output, and for UI changes a screenshot
     (light + dark menu bar if the icon is affected). -->

## Checklist

- [ ] `swift build && swift test` pass locally.
- [ ] Branch name follows `<type>/<slug>` (e.g. `fix/empty-session-card`).
- [ ] Tests added/updated where reasonable.
- [ ] No new third-party dependencies.
